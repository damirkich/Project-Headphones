# Configure npm for cross-platform compatibility
fund=false
audit=false
# Handle optional dependencies more gracefully
optional=true
# Skip platform-specific optional dependencies that might cause issues
engine-strict=false
